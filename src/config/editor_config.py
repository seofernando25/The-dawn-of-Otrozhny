"""Level editor configuration constants."""

# Editor grid configuration
EDITOR_CONFIG: dict[str, int] = {
    "default_scale": 50,  # Default grid cell size in pixels
    "min_scale": 2,  # Minimum allowed scale
}
